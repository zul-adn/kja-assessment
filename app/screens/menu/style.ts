import { connect } from "@theme"

export default connect({
  container: "flex dark:bg-gray-900 pt-7 px-4",
  card: "items-center",
  cardTitle: "text-xl font-bold text-gray-900 dark:text-white mt-5",
})
