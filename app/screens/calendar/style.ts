import { connect } from "@theme"

export default connect({
  container: "flex dark:bg-gray-900 pt-7",
  emptyContainer: "flex items-center justify-center",
  emptyTitle: "font-bold text-2xl text-gray-900 dark:text-white",
})
