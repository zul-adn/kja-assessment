import { connect } from "@theme"

export default connect({
  container: "px-4",
  card: "row items-center",
  leftColumn: "mr-5",
  rightColumn: "flex",
  legendRow: "full row items-center justify-between mb-1",
  percentage: "text-xs font-semibold text-gray-500 dark:text-gray-400",
})
