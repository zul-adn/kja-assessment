import { connect } from "@theme"

export default connect({
  container: "w-160",
  category: "text-xs font-semibold uppercase text-gray-500 dark:text-gray-400 mb-1",
  title: "text-sm text-base font-bold text-gray-900 dark:text-white mb-2 flex-wrap",
})
